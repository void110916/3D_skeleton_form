<UserControl x:Class="_3d_obj_DrawForm.UserControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:HelixToolkit="clr-namespace:HelixToolkit.Wpf;assembly=HelixToolkit.Wpf"
            >

    <Grid>
        <!-- Place a Label control at the top of the view. -->
        <!--Label 
                HorizontalAlignment="Center" 
                TextBlock.TextAlignment="Center" 
                FontSize="20" 
                Foreground="Red" 
                Content="Model: Cone"/-->


        

        <!-- The HelixViewport3D supports camera manipulation, and can be used just like the Viewport3D -->
        <HelixToolkit:HelixViewport3D ZoomExtentsWhenLoaded="True" Margin="0,0,0,0" Name="m_helix_viewport" Initialized="m_helix_viewport_Initialized">

            <!-- Remember to add light to the scene -->
            <HelixToolkit:SunLight/>

            

        </HelixToolkit:HelixViewport3D>

    </Grid>
</UserControl>
